export enum ENDPOINTS {
    USERS = 'https://randomuser.me/api'
}

export enum HEADERS {
    REQUIRED_CUSTOM_HEADER = 'REQUIRED_CUSTOM_HEADER', // for this mock API it is not needed but maybe for real case...
}